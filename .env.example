# =================================================================
# CẤU HÌNH CHO LOCAL HOẶC DEPLOY (RAILWAY) - FOOD ORDERING SYSTEM
# =================================================================

# 1. Khóa bảo mật Flask
SECRET_KEY=supersecretkey_food_ordering_2026
FLASK_DEBUG=false

# 2. Cấu hình Cơ sở dữ liệu (MySQL / PostgreSQL)
# --- Chạy LOCAL mặc định (MySQL XAMPP / Local):
DB_USERNAME=root
DB_PASSWORD=root@123
DB_HOST=localhost
DB_PORT=3306
DB_NAME=food_ordering_db

# --- Chạy DEPLOY trên Railway (Nếu có kết nối Railway Database):
# Railway sẽ tự động tạo biến DATABASE_URL hoặc MYSQL_URL.
# Hệ thống sẽ tự ưu tiên DATABASE_URL / MYSQL_URL khi chạy trên Railway.
# DATABASE_URL=mysql://root:password@host:port/railway

# 3. Cấu hình VNPay Gateway (Sandbox)
VNPAY_TMN_CODE=1SI5X77N
VNPAY_HASH_SECRET=QPYBOSLUIPUJDYHYIVHZBMRKXEAGWYJD
VNPAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html

# URL Callback sau thanh toán VNPay:
# - Chạy LOCAL: http://127.0.0.1:5000/vnpay_return
# - Chạy DEPLOY trên Railway: https://food-ordering-system-production-1cd8.up.railway.app/vnpay_return
VNPAY_RETURN_URL=http://127.0.0.1:5000/vnpay_return

# 4. Cấu hình Kết nối RAG AI Chatbot:
# - Chạy LOCAL cùng RAG Server: http://127.0.0.1:8000
# - Chạy DEPLOY trên Railway: https://rag-food-ordering-system-production.up.railway.app
RAG_API_BASE=http://127.0.0.1:8000
